# 🎖️ Музей «Память Олёкминского края» — PHP версия

Полноценный сайт на PHP + MySQL для виртуального музея ветеранов.

## 📁 Структура проекта

```
olekminsk-museum-php/
├── public/                 # Публичная часть (DocumentRoot)
│   ├── index.php          # Главная страница
│   ├── veteran.php        # Карточка ветерана
│   ├── add.php            # Форма добавления
│   ├── bessmertny-pol.php # Бессмертный полк
│   ├── assets/            # CSS, JS, изображения
│   └── uploads/           # Загруженные фото
├── src/
│   ├── config/
│   │   └── database.php   # Настройки БД
│   └── models/
│       └── VeteranModel.php # Работа с ветеранами
├── database/
│   └── schema.sql         # Структура базы данных
└── README.md
```

## 🚀 Установка на Бегет

### Шаг 1: Создать базу данных
1. Зайди в панель управления Бегет
2. Перейди в раздел «MySQL»
3. Создай базу данных:
   - Имя: `olekminsk_museum` (или придумаешь своё)
   - Пользователь: создастся автоматически
   - Запомни пароль!

### Шаг 2: Импортировать структуру
1. Открой phpMyAdmin (ссылка в панели Бегет)
2. Выбери свою базу
3. Перейди во вкладку «Импорт»
4. Выбери файл `database/schema.sql`
5. Нажми «Вперёд»

### Шаг 3: Настроить подключение
Открой `src/config/database.php` и впиши данные:

```php
define('DB_HOST', 'localhost');
define('DB_NAME', 'имя_базы_которое_создал');
define('DB_USER', 'имя_пользователя');
define('DB_PASS', 'пароль_который_запомнил');
```

### Шаг 4: Залить файлы
1. В панели Бегет найди «Файловый менеджер»
2. Перейди в папку своего сайта (обычно `public_html`)
3. Загрузи все файлы из папки `public/`
4. Папку `src/` загрузи на уровень выше (рядом с `public_html`)

ИЛИ через FTP:
- `public/` → заливаешь в `public_html/`
- `src/` → заливаешь рядом с `public_html/`

### Шаг 5: Создать администратора

1. Открой phpMyAdmin
2. Перейди в таблицу `admins`
3. Нажми «Вставить»
4. Заполни:
   - **username**: твой логин (например, `admin`)
   - **password_hash**: хеш пароля
     - Вставь это: `$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi`
     - Это хеш от пароля **`admin123`** (потом сменишь!)
   - **email**: твоя почта (опционально)
5. Нажми «Вперёд»

Теперь можешь входить:  
🔗 `https://твой-сайт.ru/admin/login.php`  
Логин: `admin`  
Пароль: `admin123`

**Важно:** Смени пароль сразу после первого входа!

### Шаг 6: Права доступа
Убедись, что папка `uploads/` доступна для записи:
- Права (chmod): 755

## 🔐 Админ-панель

### Как работает модерация:

1. **Человек заполняет форму** на сайте (`/add.php`)
2. **Данные сохраняются** в базу со статусом `pending` (на проверке)
3. **Ты получаешь** список заявок в админке (`/admin/`)
4. **Проверяешь и нажимаешь:**
   - ✅ **Одобрить** — ветеран появляется на сайте
   - ❌ **Отклонить** — удаляется из базы
   - 👁️ **Смотреть** — предпросмотр перед решением

### Страницы админки:
- `/admin/login.php` — вход
- `/admin/index.php` — модерация заявок
- `/admin/logout.php` — выход

## 📚 Что ты изучишь

### 1. PHP основы
- `require_once` — подключение файлов
- Работа с `$_GET`, `$_POST`, `$_FILES`
- Сессии (`session_start()`)

### 2. База данных (MySQL)
- SQL запросы: SELECT, INSERT, UPDATE
- Подготовленные запросы (безопасность!)
- Индексы для быстрого поиска

### 3. MVC (упрощённый)
- **Model** — работа с данными (VeteranModel.php)
- **View** — HTML шаблоны (index.php)
- **Controller** — логика обработки (тоже в index.php)

### 4. Безопасность
- Экранирование вывода (`htmlspecialchars`)
- CSRF-токены
- Валидация данных

## 🔧 Дальнейшая доработка

### Что добавить:
1. **Telegram-уведомления** — бот пишет при новой заявке
2. **Импорт из JSON** — перенести старых ветеранов
3. **Кэширование** — ускорить загрузку
4. **SEO** — мета-теги, Open Graph
5. **Смена пароля** — в админке

### Полезные SQL-запросы:

```sql
-- Найти всех из Олёкминска
SELECT * FROM veterans WHERE settlement = 'с. Олёкминск';

-- Поиск по фамилии
SELECT * FROM veterans WHERE last_name LIKE '%Иванов%';

-- Статистика по населённым пунктам
SELECT settlement, COUNT(*) as count 
FROM veterans 
WHERE status = 'approved'
GROUP BY settlement;
```

## 💡 Как это работает

1. Посетитель открывает `index.php`
2. PHP подключается к MySQL через `database.php`
3. `VeteranModel` делает запрос: «Дай мне всех одобренных ветеранов»
4. Результат выводится в HTML
5. При клике на ветерана — открывается `veteran.php?id=123`

## 🆘 Если что-то не работает

### Ошибка подключения к БД
- Проверь данные в `database.php`
- Убедись, что база создана
- Проверь, что пользователь имеет права

### Фото не загружаются
- Проверь права папки `uploads/` (755)
- Проверь `MAX_FILE_SIZE` в конфиге
- Смотри логи ошибок

### 404 ошибка
- Проверь, что файлы в правильной папке
- Убедись, что `public/` — это DocumentRoot

## 📞 Контакты
Если застрянешь — пиши, разберёмся вместе!
